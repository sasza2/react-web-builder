import { createGlobalStyle } from "styled-components";

export const IFrameGlobalStyles = createGlobalStyle`
  body {
    font-family: Inter;
    overflow: hidden;
    margin: 0;
    padding: 0;
  }
`;
